const INITIAL_STATE = 20635765; // this is fixed at the john and ken show until I make a program page

export const selectedProgramReducer = (state = INITIAL_STATE, action) => {
    switch(action.type) {
        default:
            return state;
    }
};